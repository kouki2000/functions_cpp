#include <bits/stdc++.h>
#include <stack>
#include "Calc.h"
#include "Sort.h"
#include "MyStack.h"
#include "Search.h"
#define rep(i, n) for (int i = 0; i < (n); i++)
using namespace std;
using ll = long long;
using P = pair<int, int>;
int INF = 1001001001;

int main()
{
    Calc Cl;
    Sort So;
    MyStack St;

    return 0;
}